import express from 'express'

export class Server {
  constructor(dependencies = {}) {
    this.dependencies = this.createDependencies()
    this.app = express()
  }

  createDependencies () {
    const
  }
}
